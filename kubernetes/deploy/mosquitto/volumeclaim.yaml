kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mosquitto-data
  labels:
    app: mosquitto
    release: mosquitto
spec:
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "1Gi"
  storageClassName: "clusterstorage"
