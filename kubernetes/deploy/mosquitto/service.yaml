apiVersion: v1
kind: Service
metadata:
  name: mosquitto
  labels:
    app: mosquitto
    release: mosquitto
spec:
  type: ClusterIP
  ports:
    - port: 1883
      targetPort: mqtt
      protocol: TCP
      name: mqtt
  selector:
    app: mosquitto
    release: mosquitto
