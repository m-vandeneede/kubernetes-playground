machine:
  kubelet:
    extraMounts:
      - destination: /var/mnt/longhorn
        source: /var/mnt/longhorn
        type: bind
        options:
          - rw
          - bind
          - rshared
    nodeIP:
      validSubnets:
        - 10.70.0.0/24
  #sysctls:
  #  vm.nr_hugepages: 1024
  kernel:
    modules:
      - name: nvme_tcp
      - name: vfio_pci
  network:
    interfaces:
      - interface: eth0
        dhcp: false
        vlans:
          - vlanId: 70 # VLAN for cluster traffic
            dhcp: true
          - vlanId: 71 # VLAN for local ingress traffic
            dhcp: false
          - vlanId: 72 # VLAN for external ingress traffic
            dhcp: false
  install:
    disk: /dev/sda
    image: factory.talos.dev/nocloud-installer-secureboot/efe06b1b353c64e5582b00831867f8ec55580190d78f48b206c681b5c5a0b683:v1.13.0
---
apiVersion: v1alpha1
kind: HostnameConfig
auto: off
hostname: kube102.vandeneede.org
---
apiVersion: v1alpha1
kind: UserVolumeConfig
name: longhorn # name is used to identify the volume /var/mnt/<name>
provisioning:
  diskSelector:
    match: disk.dev_path == "/dev/sdb"
  grow: true
  maxSize: 150GB
