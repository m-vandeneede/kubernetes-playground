kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mosquitto-data
  labels:
    app: mosquitto
    release: mosquitto
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
  storageClassName: longhorn
