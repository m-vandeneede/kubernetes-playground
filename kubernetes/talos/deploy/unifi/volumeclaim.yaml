apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unifi-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn
  volumeMode: Filesystem