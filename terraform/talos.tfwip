resource "talos_machine_configuration_apply" "talos" {
    for_each = local.cluster_vms
    machine_configuration_input = data.talos_machine_configuration.talos_conf[each.key].machine_configuration
    node = each.value.hostname
}